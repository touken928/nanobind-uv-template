#
# Root of the nanobind-uv-template monorepo.
#
# There is no top-level Python distribution here. Each package lives under
# ``packages/<name>/``. uv discovers them via the workspace table below.
#
[tool.uv.workspace]
members = ["packages/*"]
